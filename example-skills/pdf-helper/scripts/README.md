# scripts/

Optional helper scripts for the pdf-helper skill.

Place executable helpers here (for example, a small Python wrapper around
pypdf for a merge/split recipe you use often) and reference them from the
body of ../SKILL.md. Keeping scripts inside the skill directory makes the
skill self-contained and portable when installed with vendor-skills.
